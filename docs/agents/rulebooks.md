# ルールブックの索引

**このファイルは生成物。手で編集しない。** 正本は `scripts/lib/catalog-registry.json` と
各ルールブックの本文で、`bash scripts/render-rulebook-index.sh` で作り直す。
最新かどうかは `scripts/check-catalogs.test.sh`（CI `hooks-test`）が検査する。

ルールブックは「守るべきことを 1 行 1 件で並べ、状態の語彙を固定し、機械が形を検査する表」。
新しく作るときは `bash scripts/new-rulebook.sh`（雛形と登録を同時に作り、その場で検査まで回す）。

**取りこぼしが起きたら、まず「限界」列を読む。** 各ルールブックが何を守らないかを先に
書いてあるので、事故の原因がそこに書いてあることが多い（詳しくは各文書の「## 限界」節）。

| ID 帯 | ルールブック | 何を並べるか | 状態の語彙 | 限界（見つからないこと） |
| --- | --- | --- | --- | --- |
| `E-xxx` | [取りこぼし台帳（E-xxx）](escaped-defects.md) | すり抜けた 1 件 = 1 行。ルールブックの限界を実際の漏れから育てるための台帳 | 未分類 / 対応中 / 解決済み / 該当なし | 気づいた漏れしか載らない。分類は自己申告で、環境を選べば何でも通せる。Bash 以外で気づいた漏れは自動では拾えない |
| `I-xxx` | [不変条件カタログ（I-xxx）](invariant-catalog.md) | 業務データが「どの経路から書かれても」満たしていなければならない条件。守る場所は原則 DB | 実装済み / 計画 / 対象外 | 条件を DB が守っているかは見るが、その条件が業務上正しいかは見ない。集計をまたぐ条件は夜間検査まで気づけない |
| `P-xxx` | [約束カタログ（AAA）](promise-catalog.md) | auth / RLS / 施設境界 / admin 境界 / AAL2 / RPC 契約の約束。守るテストの名前に ID を書いて双方向に突合する | 毎回 / 変更時 / 節目 / 一度きり | 守るテストの ID 文字列が実在するかは見るが、そのテストが本当に約束を守っているか（効き目）は見ない。効き目は RLS ミューテーションで別に測る |
| `Q-xxx` | [上限（クォータ）の棚卸し（Q-xxx）](quota-inventory.md) | 正規の権限を持つ 1 人が、その権限のまま**大量に**使ったときに何が起きるかを経路ごとに並べた表（issue #757 の 32）。 | 上限あり / 上限なし（意図） / 要設定 / 未測定 | 上限があるかしか見ない。値が妥当かも、上限を通らない経路があるかも見ない。外部サービス側の既定はここからは確かめられない |
| `R-xxx` | [施設ロールの決めごと（R-xxx）](role-rulebook.md) | 施設ロール 1 つ = 1 行。新しいロールを足すときに決める 4 軸（読む・書く・マスタ・画面）の正本 | 実装済み / 計画 / 対象外 | ロールの軸しか見ない（MFA・所属の有無は別）。許可リストの一致は見るが、その許可リストが業務上妥当かは見ない |
| `TB-xxx` | [テーブルの決めごと（TB-xxx）](table-rulebook.md) | 新しいテーブルを作るときに決める 4 軸（RLS・ポリシー・誰が読み書きできるか・監査）の正本 | 実装済み / 計画 / 対象外 | migration の SQL しか見ないので、実 DB に手で当てた変更や Supabase の既定権限は見えない。宣言が業務上妥当かも見ない |

（6 件）
