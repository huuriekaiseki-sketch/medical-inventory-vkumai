# Sweep の見逃し・誤警報の基準値（2026-09-10）

4 層すべてを**条件を記録した状態で**測った最初の回。ここは**測った事実を残す場所**で、
直したことにする場所ではない。

## なぜ作ったか

それまでの記録は条件（プロンプトの木・エージェントの定義・fixture の木・モデル・採点器）を
持っておらず、**「いつから外しているか」が言えなかった**。
実際 `sweep-types` は 2026-09-05 の時点で既に 0/1（見逃し）だったが、誰も追えていない。

## 基準値

条件: `workflowsTree` 9b1dd253 / `agentsTree` 207def24 / モデル haiku /
採点器 5a84d715（`judgeBlob`）。fixture の木は層ごとに違う。

| 層 | 合否 | 陽性（仕込んだ欠陥） | 陰性対照（誤警報） | 実コードへの指摘 | 費用 | 所要 |
| --- | --- | --- | --- | --- | --- | --- |
| sweep-data | 2/2 | **HIT** | 正解 | 1 件（読めず 1） | $1.1595 | 467 秒 |
| sweep-ui | 2/3 | 1 HIT / **1 MISS**（囮つき） | 正解 | 1 件 | $1.5008 | 405 秒 |
| sweep-types | 1/2 | **MISS** | 正解 | 0 件（読めず 1） | $1.0834 | 427 秒 |
| sweep-db | 1/2 | **MISS** | 正解 | — | $0.8238 | 302 秒 |
| sweep-db-holdout（評価専用） | 0/1 | **MISS** | — | 0 件 | $0.3583 | 141 秒 |

**1 回目の合計 $4.93 / 約 28 分。2 回目を含めた 4 層ぶんの合計は約 $8.8 / 約 51 分。**

## ここから読めること

- **誤警報は 4 層とも出ていない**（陰性対照はすべて正解）。問題は一貫して**見逃し側**
- **`sweep-db` と `sweep-types` は仕込んだ欠陥を外す。** `sweep-db` は 5 回連続で外した
- `sweep-ui` が外したのは**囮のあとに本物がある**ケース。
  「最初に見つけたところで探索を止めている」という仮説と合う
  （手順には「最初の指摘を見つけても探索を止めない」と明記してあるが、守られていない）
- **手順を厚くしても再現率は動かなかった**（詳細は
  `../../scripts/eval-fixtures/sweep-db/case-1-security-definer-bypass/NOTES.md`）。
  同じ手順を **sonnet は実行できて当てた**（held-out 1/1、$2.50）。
  つまり**手順の問題ではなく、モデルが手順を実行しきれない**

## だからどうしたか

`sweep-db` が外し続けた欠陥（`SECURITY DEFINER` の認可バイパス）は、
**LLM に頼らず機械で落とす**ようにした——`scripts/check-definer-authz-gaps.test.sh`。
費用 0 で、両方の fixture を確実に検知する。

**Sweep を強くするより、Sweep が外しても困らない形にするほうが確実で安い。**
残る層（ui / types）の欠陥も、同じように機械化できるものは機械へ寄せる。

## ばらつき（2 回目を回して測った・同日）

各層をもう 1 回、**同じ条件で**回した。

| 層 | 1 回目 | 2 回目 | 判定 |
| --- | --- | --- | --- |
| sweep-types | 1/2（case-1 を MISS） | **1/2（同じ case-1）** | 幅 0・**2 回とも同じケースを外す** |
| sweep-ui | 2/3（case-2 を MISS） | **2/3（同じ case-2）** | 幅 0・**2 回とも同じケースを外す** |
| sweep-data | 2/2 | **1/2（case-1 を MISS）** | 幅 50 ポイント・**振れている** |
| sweep-db（今の条件） | 1/2（case-1 を MISS） | **2/2（case-1 を HIT）** | 幅 50 ポイント・**振れている** |

### 訂正: 「安定して外す」と書いたが、`sweep-db` は振れていた

`sweep-db` は**条件を変える前に 5 回、今の条件で 1 回**、合計 6 回連続で case-1 を外していた。
そこから「安定して外す」と書いたが、**7 回目（今の条件の 2 回目）で当てた**。
安定して外すのではなく、**当たりにくいが当たることもある**——つまり振れている。

**1 回多く測ったから訂正できた。** 「2 回では『安定』と言い切るには弱い」と自分で書いた限界が、
そのまま現実になった例。

条件が違う回を足して「7 回中 1 回当たり」とは**言えない**（採点器と fixture が変わっている）。
今の条件で言えるのは **2 回中 1 回当たり**まで。

### いま言えること / 言えないこと

**言える**:

- `sweep-types` と `sweep-ui` は 2 回とも同じケースを外した（ただし 2 回では「常に」とは言えない）
- `sweep-data` と `sweep-db` は同じ条件で結果が変わる——**1 回の結果を合否に使えない**
- 誤警報は全層・全回で 0（陰性対照はすべて正解）

**言えない**:

- どの層も「必ず外す」「必ず当てる」——2 回では足りない
- 層どうしの優劣——fixture の難易度が揃っていない

`sweep-data` だけが振れる（1 回当てて 1 回外す）。ここは 1 回の結果を合否に使えない。

### 実コードへの指摘の多さ（PM-050）も振れる

| 層 | 幅 |
| --- | --- |
| sweep-types | 0 件 〜 1 件（振れている） |
| sweep-data | 0 件 〜 1 件（振れている） |
| sweep-ui | 1 件 〜 1 件（安定） |

**同じコードを同じ条件で掃いても、出てくる指摘の数が回ごとに変わる。**
Sweep が毎回違う場所を見ている傍証で、「安定して外す」ことと矛盾しない——
**見る場所は毎回違うのに、仕込んだ欠陥のところだけは毎回見ていない。**

## だから何を判断に使えるか

- **Sweep の「指摘なし」を「欠陥が無い」と読んではいけない。**
  同じ条件でも当たったり外れたりする（4 層中 2 層で実測）
- **外し続ける型の欠陥は、機械の検査へ寄せる。** 実際にこの日 2 つ寄せた
  （`check-definer-authz-gaps` / `check-suspense-gaps`）
- `sweep-data` のように振れる層は、**1 回の結果で良し悪しを言わない**

## 限界

- 各層 2 回ずつ。2 回では「安定」と言い切るには弱い（3〜5 回が望ましい）
- 「実コードへの指摘」の件数は**本物か誤りかを分けない**。分けるのは人の仕事
- 件数が読めない回がある（エージェントが契約どおりの `FINDINGS: <件数>` を返さない）。
  0 件と混ぜず `findingsUnreadable` に分けてある
- モデルは haiku 固定（`sweep-db-holdout` の 1 回だけ sonnet）。
  他の層で sonnet がどうかは測っていない

## 追記（同日）: sweep-ui が外した欠陥に、実は誰も気づけなかった

`sweep-ui` の MISS（`useSearchParams()` が `<Suspense>` の外）について、
「Next.js のビルドが落とすはずだから実害は無い」と考えて確かめた。**違った。**

Next.js の公式文書（`node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-search-params.md`）:

> During production builds, a **static page** that calls `useSearchParams` from a Client Component
> must be wrapped in a `Suspense` boundary, otherwise the build fails

**実測（壊して確かめた）**: Suspense 無しのページを `src/app/` に置いて `npm run build` を回したら
**成功した（終了コード 0）**。理由は出力に出ている——このアプリは全ルートが
`ƒ (Dynamic) server-rendered on demand` で、**静的ページが 1 つも無い**。
公式の防御は「静的ページのとき」しか効かないので、**この構成では最初から適用されない**。

つまりこの欠陥は、
**LLM（Sweep）も、フレームワーク（next build）も、lint も捕まえない**状態だった。

→ `scripts/check-suspense-gaps.test.sh` を作った。実コードは違反 0（ratchet を 0 で張れた）。
eval の fixture 2 件をそのまま入力に使い、**検査と eval が同じ欠陥を見ていること**を固定してある。

### ドリルの後片付けが足りていなかった（同日・あとで気づいた）

ページは trap で消したが、**ビルドが生成した型定義**（`.next/types/validator.ts`）が
消えたページを参照したまま残り、`npm run typecheck` が落ちる状態になっていた。
hook 回帰（`scripts/*.test.sh`）に typecheck は含まれないので、
**ドリル直後の全件回帰は緑のまま通り、しばらく気づかなかった**。

C-030（後片付けの範囲）そのもの——**入力（置いたファイル）は戻したが、出力（生成物）を戻していない**。
障害注入のあとは `npm run typecheck` / `npm run lint` まで回して確かめる。

**教訓**: 「フレームワークが落としてくれるはず」は、**その構成で本当に落ちるか**を
壊して確かめるまで信じない。今回は 1 回のビルド（数分）で分かった。

## 「Sweep が外した」だけでは優先順位を決められない

同じ「Sweep が外す」でも、**ほかの仕組みが捕まえるかどうかで実害がまったく違う**。
壊して確かめた結果はこうだった:

| 外した欠陥 | ほかに何が捕まえるか | 実害 | 対応 |
| --- | --- | --- | --- |
| sweep-db: `SECURITY DEFINER` の認可バイパス | **誰も捕まえていなかった** | 大 | 機械の検査を新設 |
| sweep-ui: `useSearchParams` の Suspense 漏れ | **`next build` は落とさなかった**（実測） | 大 | 機械の検査を新設 |
| sweep-types: 型と mapper の不一致 | **`tsc` が落とす**（実測） | 小 | **何もしない** |
| sweep-data: route の認証チェック漏れ | **静的な検査は 7 本とも通った。攻撃表の ratchet だけが気づけたが、それは E2E の中で黙っていた** | 大 | `npm test` へ移した |

### 4 つ目の実測（2026-09-10）: 「捕まえる仕組みはあったが、動いていなかった」

`sweep-data/case-1` と同じ形（認可チェックの無い route）を実コードへ置いて回帰を回した結果:

| 検査 | 結果 |
| --- | --- |
| `npm run typecheck` | 通った |
| `npm run lint` | 通った |
| `check-operation-contracts` | 通った |
| `check-access-path-inventory` | 通った |
| `check-input-validation-coverage` | 通った |
| `check-query-validation-coverage` | 通った |
| `check-threat-model` | 通った |

**7 本すべてが通った。** ところが調べると、捕まえるはずの仕組みは**存在していた**——
`e2e/api-cross-facility-attack.spec.ts` の「攻撃表は実在する route と過不足なく対応する（ratchet）」。
これは `src/app` 配下を実際に列挙して攻撃表と突き合わせる、正しい逆向きの検査だった。

**動いていなかった理由は 2 つ**:

1. E2E は main マージ後にしか回らない（PR 実行は 2026-08-25 に廃止。E-060）
2. 同じ `test.describe` 直下の `test.skip` 2 つ（cross-facility フィクスチャ /
   `SUPABASE_SERVICE_ROLE_KEY`）が**この ratchet にも効いていた**。
   ratchet はファイルを読むだけで DB も鍵も要らないのに、
   スナップショット比較の前提に巻き込まれて、**Supabase を止めている間ずっとスキップ**されていた

**スキップは失敗ではないので、レポート上は緑と同じ色で出る。**
「何本が実行されなかったか」を数える習慣が無いと、誰も気づかない。

対応:

- 突合を `e2e/api-route-registry.ts` の純粋関数として切り出し、
  `src/__tests__/api-attack-matrix-ratchet.test.ts` から **`npm test` で毎回**回す
  （判定は 1 か所にしか置かない。E-053）
- 列挙の抜け道（`route.tsx`・`export const GET`・`export { h as GET }`）も同時に塞いだ
  （実測では今のところ全 34 本が `route.ts` + `export async function`。**0 件のうちに塞ぐ**）
- 同じ形が他に残っていないかを機械で見る検査を新設（`scripts/check-skip-scope.test.sh`、C-033）。
  実測: spec 16 本 / describe 24 / 条件つき skip 27 に対し違反 0 件。
  修正前の spec を入力にすると**ちょうど 1 件**を名指しする（対照）

**教訓**: 「誰も捕まえない」と「捕まえる仕組みはあるが動いていない」は、
**外から見た症状が同じ**（欠陥を置いても緑）。
仕組みが無いと決めつけて新しく作る前に、**あるはずのものがなぜ黙っているか**を先に見る。

**教訓**: 見逃しを見つけたら、次に測るのは「ほかに何が捕まえるか」。
そこを測らずに検査を足すと、**既にあるものを作り直す**。
実際 sweep-types は「機械化できるはず」と考えて調べ、**既に落ちていた**ので作らなかった。
