# 取りこぼし台帳（E-xxx）

**すり抜けた 1 件 = 1 行。** ルールブックに書いた「限界」が的外れかどうかは、書いた時点では分からない。
分かるのはテストやレビューで漏れが出たときだけなので、その漏れをここへ戻して限界とルールを育てる。

```
実装 → 検査 → 漏れが出る → ここに 1 行 → ルールブック・実装・テストを直す → 再検査 → 解決済み
```

**緑になっても終われない。** `未分類` の行が 1 つでもあると `npm test` が落ちる
（`scripts/lib/__tests__/escaped-defects.test.ts`）。実装を直して他が全部緑になっても、
分類していない漏れが残っている限りスイートは赤のまま。

- 落ちた検査は `scripts/record-test-failure.sh`（PostToolUse）が下書きとして
  `logs/escape-candidates.jsonl` に拾う。**判定はしない**（実装途中の RED は漏れではない）。
- 下書きを見ないまま終わろうとすると `scripts/check-escape-ledger.sh`（Stop）が聞く。
  該当なしなら `bash scripts/triage-escapes.sh --none "理由"` で記録する（黙って消す道は無い）。

## 更新ルール

- 列は固定 9 列: ID / 何が起きたか / 分類 / 何が見つけたか / 関連ルールブック / 誰が値を決めたか /
  直したこと / 再発防止 / 状態。列の中に `|` を書かない。
- ID は `E-` + 3 桁。区分ごとに 10 刻み（権限・認可 01x / 記録・監査 02x / 検査の運用 03x）。
- **分類**は 4 語のみ: 実装バグ / テスト不足 / ルールブック不足 / 環境。
  `ルールブック不足` を選んだら、関連ルールブックの行と `## 限界` を直すまで `解決済み` にしない。
- **何が見つけたか**（検出源）は下の語彙から選ぶ。**独立性は検出源から決まる**ので行ごとに書かない。
- **誰が値を決めたか**: `domain_owner` / `security_review` / `product_owner` /
  `AI 提案（人が承認）` / `AI 提案（未承認）` / `該当なし`。
  上限・権限・保持期間・可視範囲のように**値を決めた**行では必須。
  `AI 提案（未承認）` が多い箇所は、ルール・実装・テストが同じ生成元に依存している合図。
- **再発防止**列にはバッククォートでパスを書く。`解決済み` の行はここが必須
  （`scripts/lib/check-catalog.mjs` が状態と突き合わせ、パスの実在も見る）。
- 状態は 4 語のみ: 未分類 / 対応中 / 解決済み / 該当なし。

## 検出源と独立性

「独立」とは手段の名前ではなく、**ルール・実装・テストと別の前提から来ているか**で決まる。
DB 制約と RLS が同じ誤った前提から作られていれば、両方が緑でも独立ではない。

| 検出源 | 独立性 | なぜ |
| --- | --- | --- |
| 3A テスト | 同源 | ルールブックから書くので、ルールが間違っていれば期待値も間違う |
| DB 制約 | 条件つき | 別の場所で守るが、前提はルールブックと同じことが多い |
| RLS | 条件つき | 同上 |
| 層間突合 | 条件つき | ずれは見つかるが、全層が同じ思い込みなら揃って通る |
| ミューテーション | 独立 | テストの検知力そのものを測る。期待値の正しさには依存しない |
| 実経路攻撃 | 独立 | 実装の意図ではなく、外から実際に起きることを見る |
| 実測（規模・障害注入） | 独立 | 同上。件数を作って測る・依存を止めて測ると、読んで書いた前提が外れていることが出る |
| 人の判断 | 独立 | 唯一、生成元が違う |
| 実運用 | 独立 | 同上。ただし気づくのが最も遅い |

## 一覧

| ID | 何が起きたか | 分類 | 何が見つけたか | 関連ルールブック | 誰が値を決めたか | 直したこと | 再発防止 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E-010 | `schema_drift_log` が作られてから 2 か月間 GRANT が 1 行も無く、service_role でも読めなかった。それを守るテストは赤のまま放置されていた | ルールブック不足 | 人の判断 | TB（テーブルの決めごと。当時は権限の軸が存在しなかった） | AI 提案（未承認） | service_role にだけ SELECT を与える migration を足した | 4 軸を 1 枚にまとめ、宣言と実態を両方向で突き合わせる `supabase/migrations/__tests__/table_registry.test.ts` | 解決済み |
| E-011 | **障害時の判断根拠が間違っていた。** fail-open 棚卸しの F-004 は「止まるもの」を Supabase Auth の MFA API と書いていたが、`getAuthenticatorAssuranceLevel()` は **GoTrue を呼ばず**手元の JWT の `aal` クレームを読むだけだった（GoTrue を止めても 2 ms で成功）。挙動の穴ではない（aal2 のトークンを持つ人が通るのは正しい）が、**「Auth が落ちたら MFA ガードが閉じる」という読み方は誤り**で、実際に閉じる条件（トークンが無い・壊れている）と食い違っていた | ルールブック不足 | 実測（規模・障害注入） | F-004（fail-open の棚卸し。「止まるもの」列） | AI 提案（未承認） | 「止まるもの」を「手元の JWT の aal クレーム」に直し、実測値を添えた | `supabase/__tests__/fault-injection/fail-open.faultinjection.test.ts` | 解決済み |
| E-012 | **「壊れたら必ず落ちる」と書いてあったが、落ちない場合があった。** fail-open 棚卸しの F-011 は「SQL 関数の例外は文ごと失敗する」としていたが、permissive なポリシーは OR で合成されるので、**別のポリシーが先に通すと壊れた述語は評価されない**（実測: `is_facility_member` を例外を投げる版に差し替えても、writer 側のポリシーを持つ staff は1 件読めた。viewer は P0001 で文ごと失敗した）。あわせて `has_aal2` の説明も不正確で、「`aal` が無ければ false」ではなく **TOTP 登録済みの利用者から `aal` を剥いだときだけ false**だった（未登録には `aal` を見ずに true）。**壊れていることに気づけない読み方**をしていた | ルールブック不足 | 実測（規模・障害注入） | F-011（fail-open の棚卸し。例外の伝わり方と has_aal2 の条件） | AI 提案（未承認） | 条件つきであることと has_aal2 の実際の条件を書き、両方の場合を実測で残した | `supabase/__tests__/fault-injection/db-internal.faultinjection.test.ts` | 解決済み |
| E-020 | 明細（`*_items`）の監査行が、その施設の人に 1 件も見えていなかった。行は残っていたが読めなかった | ルールブック不足 | 3A テスト | P-060 / P-062（「残る」は書いてあったが「読める」は書いていなかった） | domain_owner | トリガーが親から `facility_id` をたどるようにした | 16 表 × 3 操作で監査行がちょうど 1 行かを測る `supabase/__tests__/integration/audit-completeness.integration.test.ts` | 解決済み |
| E-021 | **監査ログは残っていたが、実用的に読めなかった。** 画面の既定の問い合わせ（絞り込みなし・新しい順・50 件）を支える索引が 1 本も無く、`LIMIT 50` でも毎回**表全体を走査**していた（20 万行で 5,791 blocks / 19.5 ms）。同じ形の `access_denials` と `privileged_operations` には最初から両方あり、**先に作った `audit_log` にだけ無かった**。「行はあるが読めない」は E-020 と同型で、今回は権限ではなく規模で読めなくなる | ルールブック不足 | 実測（規模・障害注入） | Q-004（量の上限。「どう読まれるか」を決める軸が無かった） | AI 提案（未承認） | `(occurred_at DESC)` と `(actor_id, occurred_at DESC)` を足した（5,791 → 5 blocks） | `scripts/check-append-only-log-indexes.test.sh` | 解決済み |
| E-022 | **共有バケツ＋消せない表で、テストが履歴に依存していた。** `denial-anomaly-nightly` の「未認証の拒否は guard ごとにまとめて数える」は、`actor_id` が null の拒否が**全実行で同じバケツ**に入り、`access_denials` が append-only で消せないため手元の DB に積み上がる。1 時間窓で「増えたこと」を見ていたので、山が別の時間帯にあると before と after が同じ値になって落ちる（実測: 22:00 に 20 件・23:00 に 8 件でどちらも 22）。E-031 と同じファイルの別の問題 | テスト不足 | 実運用 | 未（テストの書き方であってルールの穴ではない） | 該当なし | 増減ではなく「1 行にまとまり guard と reason で数えられているか」を見る形にした | `supabase/__tests__/integration/denial-anomaly-nightly.integration.test.ts` | 解決済み |
| E-023 | **消せない表が積み上がると、全件を取るテストが静かに壊れる（E-022 と同じ型で 2 回目）。** `audit-completeness` は `audit_log` を**全件**取って差分を数えていたが、PostgREST の既定上限（1,000 行）に当たると**古い順に 1,000 件**が返り、いま書いた行が切り落とされる。結果は「増えた監査行 0」＝**監査の取りこぼしに見える**（実測: `user_facilities` が 1,195 行で発火。しきい値を越えた瞬間から毎回落ちる）。`audit_log` は append-only で消せないので、**手元で使うほど壊れる**方向にしか進まない | テスト不足 | 実運用 | 未（テストの書き方であってルールの穴ではない） | 該当なし | 差分に要るのは直近だけなので、新しい順に 200 件だけ取って比べる形にした | `supabase/__tests__/integration/audit-completeness.integration.test.ts` | 解決済み |
| E-024 | **制約を足したのに、それを守るテストを 1 本も書いていなかった。** 20260907060000 で `products.name` / `products.maker` に上限を足したが、翌日まで統合テストも E2E も無く、**効いているかを誰も確かめていなかった**。`scripts/check-constraint-coverage.sh` は「その migration が触る表の名前が統合テストのどこかに出ているか」で数えるので、`products` を使うテストが既にある以上「穴なし」と出る。これは**同スクリプトが「既知の限界」として自分で書いていた偽陰性**そのもので、黙って間違えたのではなく、書いてある限界に誰も手を打っていなかった。実測すると、入口の検査を外した状態では利用者に **500「製品の作成に失敗しました」**が返り、長さの問題だと分からない | テスト不足 | 人の判断 | 未（制約カバレッジの限界は constraint-coverage.js に記載済み。ルールの穴ではなく手当ての漏れ） | 該当なし | DB の CHECK を実 DB で測る統合テスト 5 件と、画面から入れたときに400 と「何文字までか」が返る E2E 4 件を追加した | `e2e/products-text-limits.spec.ts` | 解決済み |
| E-030 | 統合テストが 2 件、いつからか分からないほど前から赤のままだった。検知は動いていたが誰も結果を見ていなかった | テスト不足 | 人の判断 | 未（どのルールブックの担当でもなかった） | AI 提案（未承認） | 権限を直して全件を緑に戻した | 実行を包んで exit code から結果を記録し、木が変わったら次のセッションで聞く `scripts/check-integration-freshness.sh` | 解決済み |
| E-031 | `denial-anomaly-nightly` のテストが、前のテストが resolved にした行が履歴として残ることを見ておらず、全件を数えて「1 行のはず」としていた。**元のブランチでも落ちていた**（マージ差分ゼロで確認） | テスト不足 | 3A テスト | 未（テストの書き方であってルールの穴ではない） | 該当なし | 未解決の行だけを数えるようにした | 全件を通した結果を記録し、赤いまま終われなくする `scripts/check-integration-freshness.sh` | 解決済み |
| E-032 | `rate_limit_counters` は 20260907000005 が `REVOKE ALL ... FROM anon, authenticated` しか書いておらず、**PUBLIC と service_role が残っていた**（Supabase の既定権限で service_role は ALL を持つ） | ルールブック不足 | 層間突合 | TB-052（テーブルの決めごと。4 軸の「誰が読み書きできるか」） | AI 提案（未承認） | 明示 REVOKE のうえ service_role にだけ SELECT を戻す migration を足した | `supabase/migrations/__tests__/table_registry.test.ts` が「既定に答えを委ねない」を毎 PR 検査する | 解決済み |
| E-033 | マスタの書き込みには aal2 が要る（P-033）のに、**所属と役割の変更には要らなかった**。パスワードだけ奪われた admin が共犯者を admin に昇格できた。原因は経路の違いで、権限の変更だけが service_role（RLS を通らない鍵）で書かれていた | ルールブック不足 | 人の判断 | P-033 / P-035（aal2 の要求が経路によって抜けていた） | domain_owner | user_facilities に `is_admin() AND has_aal2()` のポリシーを作り、管理 API を利用者の JWT に切り替えた | service_role で書く経路を 1 枚に並べ、API に認可の判定を必須にする `src/__tests__/privileged-writes.test.ts` | 解決済み |
| E-034 | **拒否の記録の中身を誰も見ていなかった。** `recordAccessDenial({ guard, reason, actorId, facilityId })` を `recordAccessDenial({})` や `guard: ""` に書き換えても、`admin-auth.ts` / `require-facility-access.ts` の全テストが緑のままだった。この語彙は `check_denial_anomalies`（夜間の異常検知）が数えているので、静かに空文字へ化けると「誰がどの境界で弾かれたか」を読む仕組みごと死ぬ | テスト不足 | ミューテーション | P-063（拒否の記録） | 該当なし | 記録の中身（guard / reason / actorId / facilityId）を `toHaveBeenCalledWith` で固定するテストを 3 ファイルに追加 | `stryker.config.json` の `thresholds.break` を 94 に引き上げ、同じ形の変異が生き残ったら終了コード 1 にする | 解決済み |
| E-035 | **W-011 対応で足した `assertAdminAal2` に単体テストが 1 本も無かった。** 35 変異すべてが未カバーで、`return false` を `return true` に書き換えても全テストが緑だった（=「確かめずに特権操作を通す」に化けても気づけない）。route 側のテストは `@/lib/admin-auth` ごとモックしていたため触れていなかった | テスト不足 | ミューテーション | W-011（service_role で書く経路） | AI 提案（未承認） | fail-closed の 9 経路と弾く順序を固定するテスト 14 件を追加（19.61% → 100%） | `stryker.config.json` の break threshold を 94 に。**同じ日に自分で開けた穴を ratchet が捕まえた** | 解決済み |
| E-036 | **監査画面の絞り込みが 1 件も守られていなかった。** `listAccessDenials` は `if (query.facilityId) q = q.eq(...)` を `if (false)` にしても、`.range(offset, offset + limit - 1)` の件数を狂わせても、`if (error) throw error` を消しても全テストが緑だった。拒否の記録は事故の調査で使う唯一の手段で、静かに効かなくなると「拒否 0 件」という**安全に見える誤った結論**が出る | テスト不足 | ミューテーション | 未（監査画面はどのルールブックの担当でもなかった） | 該当なし | 列名・件数の範囲・絞り込みの有無・エラーの再送出を固定するテスト 8 件を追加（65% → 100%） | `stryker.config.json` の break threshold を 94 に | 解決済み |
| E-037 | **`aal2` を要求するポリシーを書いたが、それが効き続けることを誰も測っていなかった。** `is_admin() AND has_aal2()` から `has_aal2()` **だけ**を外す変異（RM-011〜RM-013）が 3/4 で生き残った。特に `user_facilities` は **E-033（パスワードだけ奪われた admin が共犯者を昇格できた事故）を塞いだ本体**で、その migration の存在理由そのものが測れていなかった。`access_denials` はコメントに「aal1 で読めないことは blast-radius が測る」と書いてあったが、**blast-radius はその表を見ていなかった** | テスト不足 | ミューテーション | P-035 / P-063 / P-066（aal2 を要求する約束） | 該当なし | 各表に「aal1 の admin では届かない」と「aal2 まで上げれば届く（対照）」を対で追加（7 件） | `scripts/lib/rls-mutants.json` に aal2 だけを外す変異を 3 件追加し、14/14 撃墜を確認 | 解決済み |
| E-038 | **変異が自分の宣言を実装できていなかった（等価変異）。** RM-009「価格履歴の直接 INSERT を許す」はポリシーだけを緩めていたが、`price_histories` は `GRANT SELECT` しか無いので INSERT は GRANT の段階で拒否される。つまり変異が**何も変えていなかった**。テスト側も 42501（GRANT レベルの拒否）を期待していたため、**変異とテストが「守っているのはポリシーだ」という同じ思い込みに乗っていた** | テスト不足 | ミューテーション | P-051（価格履歴はトリガーだけが書く） | 該当なし | 変異に `GRANT INSERT` と PostgREST のスキーマ再読み込みを足して実効化した（RM-010 と同じ形） | `scripts/lib/rls-mutants.json` の RM-009。初回計測で「倒した」と数えていたのは誤りだった | 解決済み |
| E-039 | **秘密の走査は `git ls-files` しか見ないので、書いた直後の緑は当てにならない。** 履歴走査の空振り防止に使う偽の値を 2 つリテラルで書いてしまい、**書いた時点では未追跡なので検査は緑**、`git add` して初めて「秘密がある」と自分の検査が報告した。1 つはコミット後に気づいたため**履歴からは消せない** | テスト不足 | 人の判断 | 未（走査の限界に「未追跡のファイルは見えない」が書かれていなかった） | 該当なし | 偽の値を組み立てて作る形にし、履歴に残った 1 件はハッシュで許可リストへ登録した（理由の無い登録は効かないことをテストで固定） | `scripts/check-secret-leak-history.test.sh` | 解決済み |

## 読み方

まず `未分類` と `対応中` を見る。次に **`誰が値を決めたか` が `AI 提案（未承認）` の行**を見る。
そこはルール・実装・テストが同じ生成元に依存していて、揃って間違える余地が残っている。

`何が見つけたか` を数えると、投資先が分かる。

- **3A テストばかり**が見つけている → 独立根拠（ミューテーション・実経路・人のレビュー）が足りない
- **独立根拠ばかり**が見つけている → 3A の書き方が甘い。ルールブックから期待値を写すだけになっていないか
- **人の判断ばかり**が見つけている → 機械の検査が届いていない領域がある

2026-09-08 時点の 18 件は
「ミューテーション 5 / 人の判断 5 / 実測（規模・障害注入）3 / 3A テスト 2 / 実運用 2 / 層間突合 1」。

当初（2026-09-07 の 11 件）は**機械が自力で見つけたのが E-032 の 1 件だけ**で、
「人の判断ばかりが見つけている → 機械の検査が届いていない領域がある」に当てはまっていた。
翌日にミューテーションと実測を回したことで、独立根拠が 9 件（5 + 3 + 1）と過半になった。
**次に足りないのは実経路攻撃**で 0 件のまま。

**「限界に書いてあることは、書いただけでは防げない」**（E-024）。
制約カバレッジは偽陰性が残ることを自分で明記していたが、その穴からちょうど 1 件すり抜けた。
限界を書くのは**気づいたときに疑える**ようにするためで、防ぐ手当てとは別物。
制約ごとに「守るテスト」を宣言させる形にすれば閉じるが、テストは制約名を書かない
（23514 を見るだけ）ので、宣言の置き場所を決める必要がある。**次に同じ形が出たら着手する**。

**同じ型が 1 日で 2 回出た**（E-022 / E-023）。どちらも
「**消せない表（append-only）に積み上がった行**に、テストが暗黙に依存していた」形で、
しきい値を越えた瞬間から毎回落ちる。手元で使うほど壊れる方向にしか進まないので、
**3 回目が出たら機械検査にする**（「監査系の表を上限なしで全件取らない」）。
いまは 2 件なので、型として書き留めるところまでにする。
同日にミューテーション計測を回して E-034〜E-038 の 5 件が増え、独立根拠の側が 6 件になった。
**うち 2 件（E-037・E-038）は「検査そのものの穴」**で、検査を足したこと自体が次の穴を見せた（aal2 の層が測れていない・変異が実装できていない）。**3 件とも「テストはあるが結果の一部を誰も見ていない」形**で、
3A テストからは原理的に見えない（テスト自身が見ていないものを、テストは指摘できない）。
`AI 提案（未承認）` は 11 件中 4 件。値をこちらで決めた箇所ほど、
ルール・実装・テストが同じ生成元に依存していて、揃って間違える余地が残っている。

## 限界

- **分類は自己申告のまま。** 機械が強制できるのは「分類を書いたか」までで、その分類が正しいかは
  判定できない。`環境` を選べば何でも通せる（同じテストで `環境` が続くならフレーキーの合図）。
- **Bash 以外で気づいた漏れは自動で拾えない。** レビューでの指摘・実運用での発覚は手で足すしかない。
  2026-09-07 の 3 件のうち 2 件がこれで、機械は 1 件も拾っていない。
- **ミューテーションは「今の値を固定した」までしか言わない。** E-034〜E-036 で足したテストは
  「その行が今の値であること」を守るだけで、その値が業務として正しいかは何も言わない。
  値の妥当性は [`design-questions.md`](./design-questions.md)（作る前に人に聞く）側の担当。
  また、この計測は**人が `npm run test:mutation` を打たないと動かない**
  （[`mutation-testing.md`](./mutation-testing.md) の「起動の引き金」）。
- **「見つけた」と「起きていた」は違う。** ここに載るのは気づいたものだけで、
  まだ誰も気づいていない漏れは 1 行も載らない。件数の少なさを健全さの証拠に読まないこと。
- **下書きから台帳へ移す一歩は止められない。** `npm test` が止めるのは「台帳に未分類がある」状態で、
  「そもそも台帳に書かない」ことは止められない（Stop hook が聞き続けるだけ）。
