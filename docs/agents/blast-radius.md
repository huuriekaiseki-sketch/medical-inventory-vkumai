# 乗っ取り後の到達範囲（B-xxx）

正規の資格情報が漏れた前提で、その立場が**どこまで届き**、**何が痕跡に残り**、**何が止めるか**を
立場ごとに測った表（issue #757 の 39）。脅威モデル（`threat-model.md`）が「誰が・どこから」を
書くのに対し、ここは「入られた後、どこまで広がるか」を数字と実測で書く。
アクセス経路の棚卸し（`access-path-inventory.md`）が鍵の所在、ここが鍵ごとの被害。

## 更新ルール

- 列は固定 7 列: ID / 立場（持っているもの） / 読める範囲 / 書ける範囲 / 痕跡（検知） / 止めるもの / 状態。列の中に `|` を書かない。
- ID は `B-` + 3 桁。区分ごとに 10 刻み（利用者の資格情報 00x / 運用の鍵 01x / 開発・配布の経路 02x）。欠番は詰めない。
- 状態は 4 語のみ:
  - 限定済み: 範囲が意図した広さに収まっていて、それを守る実測テストがある
  - 広い（意図）: 広いが設計としてそう決めた。理由を「読める範囲」か「止めるもの」に書く
  - 要縮小: 広すぎる。`#757-N` を必ず書く
  - 未測定: 測っていない。`#757-N` を必ず書く
- 「止めるもの」列はバッククォートでテスト・仕組みのパスを書く。無い行は `未`。
  `scripts/check-blast-radius.test.sh`（CI `hooks-test`）が列数・ID・状態・パスの実在・
  「限定済み」のテスト必須・`#757-N` の有無を検査する。
- **更新の引き金**: 新しい鍵・ロール・外部連携の追加、RLS ポリシーの緩和、テナント数の桁が変わったとき、
  実際のインシデント。

## 一覧

### 利用者の資格情報（00x）

| ID | 立場（持っているもの） | 読める範囲 | 書ける範囲 | 痕跡（検知） | 止めるもの | 状態 |
| --- | --- | --- | --- | --- | --- | --- |
| B-001 | admin のパスワードだけ（TOTP なし。MFA は登録済み＝aal1 セッション） | **全施設の全業務データ**。他施設の発注・患者 ID・イニシャル・医師名・仕入価格まで主キー直指定で読める。読み取りに `has_aal2()` は掛かっていない | 無し（マスタ・施設スコープとも aal2 が要る） | **残らない**。SELECT は監査トリガーに来ない（#757-24 の残り）。Supabase の API ログには残るが未確認（D-022） | TOTP（aal2）を要求するのは書き込みだけ。読み取りを止めるものは無い | 広い（意図） |
| B-002 | 同上（施設スコープの表への書き込み） | 同上 | 無し。自施設・他施設とも INSERT は拒否、UPDATE / DELETE は 0 行 | 書けないので残らない | `facility_writer_or_admin` ポリシーの `has_aal2()`（#623） | 限定済み |
| B-003 | 同上（マスタと施設の新規作成） | 同上 | 無し。2026-09-06 までは **products などマスタ 4 表を aal1 のまま改ざん・削除できた**（実測で発見）。products の削除は distributor_products → hospital_prices へ CASCADE し、全施設の仕入価格が消える | 書き込みは `audit_log` に actor 付きで残る（P-060） | `products_write` などの `has_aal2()`（P-033、migration 20260906000008） | 限定済み |
| B-004 | admin のパスワードと TOTP の両方（完全な乗っ取り） | 全施設 | 全施設の業務データとマスタ。施設の削除だけは DELETE ポリシーが無いのでクライアントからは不可（service_role のみ） | 全ての書き込みが `audit_log` に actor 付きで残る。読み取りは残らない | 監査ログ（P-060〜P-062、消せない）。範囲そのものを狭める仕組みは無い | 広い（意図） |
| B-005 | staff（施設 A のみ）のパスワードと TOTP | 施設 A のみ。他施設は 0 件（RLS） | 施設 A のみ | `audit_log` に残る | 施設境界の RLS（P-010〜P-017）。`supabase/__tests__/integration/case-orders-rls-idor.integration.test.ts` ほか施設境界の統合テスト群 | 限定済み |
| B-006 | viewer のパスワードと TOTP | 施設 A のみ | 無し | 読み取りは残らない | `is_facility_writer` が viewer を除く。`supabase/__tests__/integration/rbac-viewer-role.integration.test.ts` | 限定済み |
| B-007 | 退職者・降格済みの古いセッション（アクセストークンが手元にある） | 権限は次のリクエストから消える。JWT の `user_role` クレームだけ最大 1 時間古い（UI の出し分けのみ） | 同上 | — | RLS が毎回 DB を引く（P-023）。`supabase/migrations/20260628010000_add_role_to_user_facilities.sql` の is_admin / is_facility_member が毎回 user_facilities を読む。降格・削除の実測は 27 番の PR | 限定済み |

### 運用の鍵（01x）

| ID | 立場（持っているもの） | 読める範囲 | 書ける範囲 | 痕跡（検知） | 止めるもの | 状態 |
| --- | --- | --- | --- | --- | --- | --- |
| B-010 | service role key（Vercel の環境変数・CI・開発機） | 全施設。RLS を通らない | 全施設。`audit_log` の UPDATE / DELETE / TRUNCATE だけはトリガーが拒否する | 書き込みは `audit_log` に `actor_role = service_role`・`actor_id = null` で残る。誰が使ったかは分からない | 鍵の保管（四半期棚卸しのランブックは 36 番の PR）。ローテーションの実測は #757-29 | 要縮小（#757-29: ローテーション手順の実測と、Vercel 側の参照者の棚卸し） |
| B-011 | Supabase ダッシュボードのログイン | 全施設 + スキーマ変更 + 鍵の再発行 | 同上 | DDL は日次のスキーマドリフト検知が issue 化する。データ変更は監査ログに残る。設定変更は残らない | ダッシュボードの MFA（未確認）。設定の期待値比較は #757-35 | 未測定（#757-35: ダッシュボード側の MFA と操作ログを確認する） |
| B-012 | anon key（公開されている前提の鍵） | RLS の SELECT ポリシーが `true` のマスタ（商品・カテゴリ・代理店商品・互換性）。業務データは 0 件 | 無し | — | RLS と、公開 RPC の GRANT 棚卸し（P-043〜P-045）。`supabase/__tests__/integration/rpc-boundary.integration.test.ts` | 限定済み |

### 開発・配布の経路（02x）

| ID | 立場（持っているもの） | 読める範囲 | 書ける範囲 | 痕跡（検知） | 止めるもの | 状態 |
| --- | --- | --- | --- | --- | --- | --- |
| B-020 | GitHub リポジトリへの push 権限 | コードのみ（業務データは無い） | main へ直接 push できる（ブランチ保護なし。Free プラン） | git 履歴 | CI は落ちても止められない（#757-6）。migration は日次のドリフト検知が拾う | 要縮小（#757-6: required check の有効化はプラン判断） |
| B-021 | 開発機（ローカル Supabase・`.env.local`） | ローカルの架空データのみ。本番データは複製しない運用 | ローカルのみ | — | 運用（本番データを開発機へ持ってこない）。`scripts/check-secret-leak.test.sh`、`scripts/check-pii-leak.test.sh` | 限定済み |
| B-022 | プラグイン配布物（marketplace）の改ざん | 導入先の開発環境 | 導入先の hook 経由で任意コード | 生成の決定性検査（`--check`）。配布物とソースのハッシュ一致は未 | 生成の決定性 | 要縮小（#757-37: 配布物の同一性検査、#757-30: 供給網の演習） |

## 読み方

- **一番広いのは B-001**。パスワード 1 つで全施設の患者情報が読め、痕跡が残らない。読み取りにも
  aal2 を要求すれば塞がるが、MFA 未登録の利用者が一切使えなくなるため、いまは「広い（意図）」として
  記録する。塞ぐなら「読み取りの監査」（#757-24）と「読み取りの aal2 化」のどちらを先にやるかを決める。
- **B-003 は実測で見つけて塞いだ**。設計上は #623 で「業務データの書き込みには TOTP が要る」と
  決めていたが、マスタ 4 表が漏れていた。カタログの行を読むだけでは気づけず、実 DB で測って分かった。
- **書き込みは全て `audit_log` に残る**（B-003・B-004・B-010）。読み取りはどの立場でも残らないので、
  「いつ漏れたか」は今のところ答えられない（#757-24）。
