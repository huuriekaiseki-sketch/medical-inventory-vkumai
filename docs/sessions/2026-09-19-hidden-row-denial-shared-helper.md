# 2026-09-19 hidden-row-denial-shared-helper

## 30秒サマリー
- 変更概要: 記録が env 未設定で黙って落ちる 3 経路を塞ぎ、4 ファイルにコピペされていた
  service_role クライアント生成を `src/lib/security/service-role-client.ts` へ一本化した（issue #793、PR #798）
- リスク: 中（`src/lib/security/` の 4 ファイル。ただし**認可の判定は 1 行も変えていない**——
  変えたのは記録の配管と、その失敗が黙るかどうかだけ）
- 変更領域: ロジック（セキュリティ層）／テスト／ドキュメント
- 証拠状態: 実測 8 件 / サンプル・未検証 0 件
- 影響範囲: 拒否記録・隠れ行の記録・特権操作の記録・レート制限の 4 経路。利用者から見た挙動は変わらない
- ロールバック可能性: 高（revert で戻る。ただし無音の穴も戻る）

## どう確認したか

`bash scripts/derive-test-selection.sh origin/main --format table` は **route: deep** を返し、
その要求を全て実施した。詳細な 04 表は PR #798 の本文にある。

| 種別 | 状態 | 結果・証跡 |
| --- | --- | --- |
| 型検査 / lint | ✅ 実施（手動） | `npx tsc --noEmit` エラー 0（**一度落として直した**）、`npm run lint` 警告 0 |
| unit | ✅ 実施（自動テスト: パス） | `npm test` → 250 ファイル / 2312 件 |
| hook 回帰 | ✅ 実施（自動テスト: パス） | `scripts/*.test.sh` 全件で落ちた検査 0 件 |
| RLS/IDOR 統合（実 DB） | ✅ 実施（自動テスト: パス） | `npm run test:integration` → 49 ファイル / 377 件・1 skip |
| 直接攻撃の実測 | ✅ 実施（自動テスト: パス） | `e2e/api-cross-facility-attack.spec.ts` → 1 passed |
| 障害注入 | ✅ 実施（自動テスト: パス） | `check-fail-open.test.sh` + proxy/supabase/admin-status → 77 件 |
| 生成型の鮮度 | ✅ 実施（自動テスト: パス） | `check-generated-supabase-types.sh` → up to date |
| build / 依存監査 | ⬜ 未実施 | CI に委ねた（package.json 未変更） |
| E2E 全体 / スキーマドリフト / フレーキー / 規模 | ➖ 今回不要 | 節目の種別 |
| 依存差分レビュー | ➖ 今回不要 | package.json / package-lock.json に触れていない |
| 冪等性 / 同時実行 / 業務不変条件 | ➖ 今回不要 | 該当する RPC・migration・リポジトリに触れていない |

**RED 方向を 2 種類実測**した。共有ヘルパーの警告を無効化すると 4 経路 + 共有ヘルパーで **6 件**が落ち、
`src/lib/security/` に 5 個目のコピペを仕込むと構造テストが**ファイル名を名指しして**落ちる。

## フロー実行時間
- Phase 1（調査）:    24分54秒
- Phase 2〜5（実装）: 13分20秒
- AI稼働合計:         38分14秒
- セッション総時間:   60分48秒（人間レビュー待ち含む）

## フロー実行統計

### Phase 1 調査
- Sweeper: 67台 × 3ラウンド
- 指摘あり軸数: 18 / 4

### Phase 2〜5 実装・検証
- implementer: 0台（並列）
- integrator: 1台
- reviewer: 0台（並列）
- 合計エージェント: 0台
- 実装成功: 1 / 0グループ

## Loop Observability要約
このセッション中の新規記録なし

## 結果

**PR**: #798（issue #793）。記録が env 未設定で黙って落ちる 3 経路を塞ぎ、4 ファイルの
service_role クライアント生成を `src/lib/security/service-role-client.ts` へ一本化した。

### うまくいったこと

- **#791 の修正が実地で効いた。** 同じ deep ルートを回して、終了時の HEAD と作業ツリーが
  開始時のまま（`866e36aa` / dirty 空）。前回はまったく同じ条件で製品コード 4 ファイルの編集と
  2 件のコミットが起きていた。67 体すべてが `agentType` を持つことも実測（内訳:
  adversarial-verify 30 / judge-panel 10 / reviewer 7 / completeness-critic 4 / proposer 3 /
  sweep-* 各 3 / spec-drafter 1）
- **RED 方向を 2 種類とも実測した。** 警告を無効化すると 4 経路 + 共有ヘルパーで 6 件が落ち、
  5 個目のコピペを仕込むと構造テストがファイル名を名指しして落ちる
- **deep 判定が要求した検証を全部回した。** 統合テスト 49 ファイル / 377 件、他施設ユーザーによる
  直接攻撃の総当たり、生成型の鮮度、障害注入 77 件

### 問題・気になった点

- **停止①の判断材料が 1 つ誤っていた。** 「reset 関数は両方デッドコード」と提示して承認を得たが、
  deep ルートの調査が `grep -rn ... src` で確かめており **`supabase/__tests__/` を見ていなかった**。
  実際には `privileged-operations-rls-idor.integration.test.ts:272,279` が呼んでいる。
  **型検査が捕まえた**ので実装中に訂正できたが、67 体・293 万トークンかけた調査が
  走査範囲を 1 ディレクトリに絞ったせいで間違えた（C-040 の型）。
  人に承認させる材料そのものが誤っていた、という点で影響が大きい
- **router が判定と起動を一体で行う。** 「router に判定させ、deep なら相談する」という段取りで
  始めたが、判定と同時に本体が走り出すので相談の機会が無かった。66 体が起動してから気づいた
- **費用が変更規模に見合っていない（#796 の 2 例目）。** 3 ファイルの配管を直すのに 293 万トークン。
  adversarial-verify が 30 件で主因なのは前回と同じ
- **issue 本文の前提が 4 点覆った。** 無音経路は 2 つでなく 1 つ、コピペは 2 ファイルでなく 4、
  fire-and-forget は存在しない、reset 関数は片方だけデッドコード。
  **issue を書いた時点の読みは、実コードで裏を取るまで前提にできない**

### 次の課題

- #796（deep ルートの費用）… 実データが 2 件そろった。adversarial-verify の 30 件から見直す
- #797（戻り値の stats）… 今回も手で数えた（67 件）。#791 で全件に `agentType` が付いたので機械化できる
- **走査範囲を `src` に絞る癖**（C-040）… 今回で 4 度目。`supabase/__tests__/` や `e2e/` を
  含めるかどうかを、走査を書くたびに意識する必要がある
