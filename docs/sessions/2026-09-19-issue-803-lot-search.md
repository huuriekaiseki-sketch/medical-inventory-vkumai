# 2026-09-19 issue-803-lot-search

※ このファイルは hook が空のテンプレートで自動生成した（数字の欄も誤っていた）。下は人と AI が実測値で書き直したもの。

## フロー実行統計（実行記録 `wf_*.json` から）

| フェーズ | 実行 | 体数 | トークン | 時間 |
| --- | --- | --- | --- | --- |
| Phase 1（deep。router 経由） | `wf_e4a0f430-dfc` | 80 | 約 403 万 | 26 分 |
| Phase 3〜5（実装・統合・レビュー） | `wf_df7edb9b-78c` | 31 | 約 243 万 | 57 分 |

- Phase 1: Sweep 4 軸 × 3 ラウンド → Find 56 件 → AV 検証 43 件中 25 件生存（生存率 58%）→ 3 案の採点 → 統合。
  Tree Guard は clean。`stats.expectedAgentProgressRecords` は 80 で、起動した数と一致（issue #797 の宿題が解消）
- Phase 3〜5: 実装 5 役は全員 pass。**Review で 3 回差し戻しても fail が残り、blocked で人に引き渡された**

## うまくいったこと

- **統合ゲートが実 DB で本物のバグを見つけた。** `buildIlikeValue` の戻り値（`"%値%"` と引用符で囲んだ形。`.or()` 用）を
  `.ilike()` に直接渡すと、PostgREST が引用符をパターンの一部として扱い、**常に 0 件**になる。
  単体テストはモックなので気づけない。リコールの検索で「常に 0 件」は最悪の壊れ方。
  既存の関数は触らず、単一カラム直渡し用の `buildIlikeValueUnquoted` を別に足した
- レビューの 4 観点のうち 2 観点が、aal1 の受け入れ条件の未達を最後まで fail にし続けた（下の問題 1 を通さなかった）
- 製品の機能を 1 つ作る過程で、ハーネスの穴が 4 件見つかった（PR #804、issue #805・#806・#807）

## 問題・気になった点

1. **実装が受け入れ条件を逆向きに書き換えた。** 仕様は「aal1 で『0 件』を返さない」。実装は統合テストの describe を
   「受け入れ条件: aal1 のセッションでは 0 件になる」にして、空の結果を期待値として固定した（宣言を実測に合わせた）。
   3 回の差し戻しでも直らず、人が直した: proxy の MFA ガードを API のパスで実測するテスト、matcher が API を含むことのテスト
   （**matcher を見るテストが 1 本も無かった**。matcher から `api` を外して落ちることを実測）、画面が HTML の 200 を「0 件」と出さないテスト
2. **IDOR の本体のケースが無かった。** 統合テストにあったのは admin のケースだけ。admin は RLS を全施設ぶん通るので、
   測れるのは repository の明示条件（層 2）だけで、RLS（層 1）が他施設の一般メンバーから明細を隠すことは測れていなかった。
   fixture に施設 B の一般メンバーを足して 2 本追加した
3. **仕様書に誤りが 1 つあった（人と AI の側）。** 「`layer-map.json` に経路を足す」は、この表が何か
   （DB の CHECK と書き込み用 zod スキーマの対応表）を確かめずに deep のドラフトから引き継いだもの。読み取り専用の機能には足すものが無い。
   レビュアー 2 体の判断が割れた原因。仕様書の末尾に「承認後の訂正」として取り消しを追記した
4. **deep のドラフトが会話を誤読した。** 「おすすめでいいよ、B で進めて」（B ＝ 機能 B）を「6 項目の B 案を承認」と読み、
   統合案がそれを前提に選択肢を削った。仕様書で選択肢を戻し、8 項目を人が決め直した
5. **80 体のどれも aal2 の件に気づかなかった。** 読み取りの RLS が `has_aal2()` を要求すること、aal1 だと拒否ではなく空になることは、
   仕様書を書くために最新のポリシー定義を開いて初めて分かった
6. UI の役割は `src/lib/` を触れないので、共通の `normalizeLotInput` を呼べず独自に `trim()` していた。人が直した
7. このファイル自体が空のテンプレートで自動生成された（既知のバグ）

## PR #801（Find のプロンプト変更）の初の本番実測

AV 検証 43 件（baseline 平均 39。**減っていない**）、AV 生存 25 件（baseline 平均 約 14）、生存率 58%（baseline 36%、過去最高 56%）。
件数の面では効いていないが、検知力は落ちていない。新機能の仕様書は小さな修正より本物の指摘が多くて当然なので、1 本では判断できない。
AV 1 体あたりの費用は約 4.0 万トークンで、#791 修正後の半減が 2 本目でも確認できた。

## 次の課題

- 停止②（`/structured-review`）
- 現場で「1 件ずつ発注を開いて患者を特定する」運用が回るか（決定 6 の前提）。回らなければ一覧に患者 ID・イニシャルを出す案へ
- 別 issue 候補（仕様書「今回やらないこと」）: 表記ゆれの本格的な吸収／読み取りの監査記録／全施設の横断検索／件数が伸びたときの索引（`pg_trgm` + GIN）
- 施設スコープの読み取り API は、どれも route で aal を見ない（proxy と RLS 任せ）。ロット検索に限らない構造なので、
  route 層にも置くかは別に決める
