# SPEC: デバイス削除ボタンに処理中表示を追加する

## Part 1 — 仕様（★人間がレビューする部分）

### 何ができるようになるか

デバイス一覧（[/products](src/app/products/page.tsx)）で「削除」を押してから実際に削除が終わるまでの間、ボタンが処理中であることが画面上で分かるようになります。

現状は「削除しますか？」の確認ダイアログでOKを押すと、通信が終わるまでボタンの見た目が変わらないまま待たされます。通信に時間がかかった場合、押せているのか分からず連打してしまう可能性があります。今回、処理中は「削除中…」というスピナー付きの表示に変え、連打・二重送信を防ぎます。

### 画面イメージ（実装前モック）📸

Claude Designで作成し、`e2e-runner`スキルの`mock-capture.mjs`で状態ごとに撮影したモックです（インタラクティブモック — クリックすると実際に状態が遷移する）。

**通常時**（削除ボタンが押せる状態）

![idle](screenshots/mock-delete-flow-helper-1-idle.png)

**削除中**（クリック直後。ボタンの代わりにスピナー＋「削除中…」を表示。この間はクリック不可）

![processing](screenshots/mock-delete-flow-helper-2-processing.png)

**完了**（削除が終わった行の表示。実装では一覧が再取得されるため、この行自体が消える）

![done](screenshots/mock-delete-flow-helper-3-done.png)

### 操作の流れ

1. デバイス一覧の該当行で「削除」を押す
2. 確認ダイアログ（`confirm('削除しますか？')`、変更なし）でOKを押す
3. 通信中は、その行の「削除」ボタンがスピナー付き「削除中…」表示に変わり、クリックできなくなる
4. 削除が成功したら一覧が再取得され、その行が消える
5. 削除が失敗したら、これまで通りアラートでエラーを表示し、ボタンは「削除」に戻る（連打防止のロックも解除する）

### 受け入れ条件（チェックリスト）

- [ ] 削除ボタンを押してから通信が完了するまで、押した行のボタンが「削除中…」表示に変わる 📸
- [ ] 「削除中…」表示の間、同じ行の削除ボタンを連打しても2回目以降の削除リクエストは送られない
- [ ] 削除が成功した場合、一覧が再取得され対象行が消える（既存の`refresh()`挙動を維持）
- [ ] 削除が失敗した場合、既存通り`alert`でエラーメッセージが出て、ボタンは「削除」表示に戻る（ロック解除）
- [ ] 他の行の削除ボタンは、ある行が処理中でも通常通り押せる（行ごとに独立した状態管理）

---

## Part 2 — 実装計画（AI用・レビュー不要）

### 実装セット一覧（依存順）

**Set A: 処理中状態の追加**（依存なし）
- 触るファイル: `src/components/products/ProductList.tsx`
- 内容:
  - `deletingId: string | null` を`useState`で追加（同時に処理中にできるのは1行のみで十分。複数行同時処理は要件に無い）
  - 削除ボタンの`onClick`ハンドラで`onDelete`を呼ぶ前に`setDeletingId(product.id)`し、削除ボタンの代わりにスピナー＋「削除中…」を表示する条件分岐を追加
  - `deletingId === product.id`の間はボタンを描画せず、処理中表示に差し替える
- テスト観点: [UI] `ProductList`のコンポーネントテストで、`onDelete`が解決するまで対象行が「削除中…」表示になること、他行のボタンは操作可能なままであることを確認

**Set B: 呼び出し元でのロック解除**（依存: Set Aの`deletingId`の受け渡し方に合わせる。実質は同一ファイル内なのでSet Aと一体）
- 触るファイル: `src/app/products/page.tsx`
- 内容: `handleDelete`が失敗した場合（`alert`表示後）に、`ProductList`側の`deletingId`をリセットできるようにする。実装方針は「`ProductList`が自分で`try/finally`により`deletingId`をリセットする」形にし、`page.tsx`側の変更は不要にする（呼び出し元の`handleDelete`のシグネチャは変えない）。
  → 調査の結果、Set A（`ProductList.tsx`内で完結）のみで実装可能と判明。Set Bは実装セットとして独立させず、Set Aに統合する。

### 並列グループ宣言

- 波1: Set Aのみ（`ProductList.tsx`1ファイルに閉じるため、単独実装。並列化する意味がないファイル数）
- 共有ファイルを触る結線なし。統合ゲート不要

### 今回のTRI/RISK判定

- 変更対象は`src/components/products/ProductList.tsx`のみ。DB・RLS・auth/facility等の高リスク領域には触れない（削除APIの呼び出しタイミング・回数は変えず、UIの表示状態のみを追加する）
- クライアント側の表示制御のみのため軽量レーン相当だが、プロダクトコード（`src/`）変更のため通常のPhase 3実装フローに乗せる

---

## Part 3 — 仕様レビュー前セルフチェック（AI用・レビュー不要）

- **モック未作成のまま文章のみで済ませていないか**: 対象外ではない（UI/UX変更あり）→ Claude Designでインタラクティブモックを作成済み、`mock-capture.mjs`で3状態（通常/削除中/完了）を撮影しPart 1に埋め込み済み ✅
- 新しい型・enum・statusフィールド: 導入なし（`deletingId: string | null`はローカルUI状態であり、下流に伝播する新しい判定値ではない）。Part 3の4項目（判定基準の欠落／下流の反応の欠落／列挙の自己矛盾／信号の意味変更）は該当なし。
