# SPEC: 消耗品カタログにメモ欄を追加する機能

## Part 1: 概要
消耗品カタログ（`consumables`テーブル）に、任意のテキストメモを保存できる`memo`カラムを追加する。

## Part 2: 実装計画

### DBスキーマ変更
`consumables`テーブルに以下のカラムを追加するマイグレーションを作成する。

- カラム名: `memo`
- 型: `text`
- NULL許容: 可（既存行への影響なし）
- デフォルト値: なし
