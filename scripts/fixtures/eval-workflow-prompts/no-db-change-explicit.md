# SPEC: 在庫一覧の表示順を施設名昇順に変更

## Part 1: 概要
在庫一覧ページのデフォルト表示順を、登録日時降順から施設名昇順に変更する。

## Part 2: 実装計画

### DBスキーマ変更
該当なし。既存のテーブル・カラムに変更は不要で、クライアント側のソートロジックのみを変更する。

### 実装セット一覧（依存順）
1. ui-impl: 一覧ページのソートキーを施設名昇順に変更（このfixtureのeval対象外）
