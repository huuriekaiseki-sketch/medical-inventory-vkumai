# 秘密らしい文字列のパターン（ERE）。1 行 1 パターン。# で始まる行と空行は無視される。
#
# WHY(1 か所に置く): 追跡ファイルの走査（check-secret-leak.test.sh）と
#      git 履歴の走査（check-secret-leak-history.test.sh）が**同じ網**を使う。
#      2 か所に書くと、片方だけパターンを足したときに「今のファイルでは止まるが
#      履歴には入れられる」という穴が開く。
#
# WHY(パターンがパターン自身に当たらない形で書く): このファイル自体が追跡ファイルであり、
#      履歴にも入る。文字クラスと量指定子で書けば、リテラルとしては一致しない
#      （例: `AKIA[0-9A-Z]{16}` は `AKIA` の直後が `[` なので自分に当たらない）。
#      新しいパターンを足すときは、両方の検査を回して自分自身を拾わないことを確かめる。
eyJ[A-Za-z0-9_-]{20,}\.eyJ[A-Za-z0-9_-]{20,}
sb_secret_[A-Za-z0-9_-]{10,}
sbp_[a-f0-9]{40}
-----BEGIN [A-Z ]*PRIVATE KEY-----
AKIA[0-9A-Z]{16}
ghp_[A-Za-z0-9]{36}
github_pat_[A-Za-z0-9_]{22,}
xox[baprs]-[0-9A-Za-z-]{10,}
sk-(ant-)?[A-Za-z0-9_-]{20,}
